# Copy to .env for local development: cp .env.example .env
DATABASE_URL=postgres://app:app-pass@localhost:5432/playhub?sslmode=disable

# Magic link auth (dev: link is logged to backend stdout)
MAGIC_LINK_BASE_URL=http://localhost:5173/auth/complete?token=
SESSION_COOKIE_NAME=lobby_session
SESSION_COOKIE_SECURE=false
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Optional JWT signing key (generate with: go run ./scripts/jwks.go)
# JWT_PRIVATE_KEY_PEM=
# JWKS_KID=lobby-dev
# JWKS_PUB_X=
