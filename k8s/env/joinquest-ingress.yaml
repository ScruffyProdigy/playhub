# Demo ingress for GKE + NGINX Ingress + cert-manager.
# Prereqs: cert-manager, cluster-issuer-letsencrypt-prod.yaml, joinquest-certificate.yaml, DNS -> ingress IP.
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: lobby
  namespace: joinquest
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - demo.joinquest.cc
      secretName: demo-joinquest-cc-tls
  rules:
    - host: demo.joinquest.cc
      http:
        paths:
          - path: /graphql
            pathType: Prefix
            backend:
              service:
                name: lobby-backend
                port:
                  number: 80
          - path: /.well-known
            pathType: Prefix
            backend:
              service:
                name: lobby-backend
                port:
                  number: 80
          - path: /healthz
            pathType: Exact
            backend:
              service:
                name: lobby-backend
                port:
                  number: 80
          - path: /
            pathType: Prefix
            backend:
              service:
                name: lobby-frontend
                port:
                  number: 80
