# One-shot: keep catalog handoff URLs and demo game modes aligned with production.
apiVersion: batch/v1
kind: Job
metadata:
  name: lobby-patch-game-handoff-urls
  namespace: playhub
spec:
  ttlSecondsAfterFinished: 600
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: patch
          image: postgres:16-alpine
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: pg-dsn
                  key: DATABASE_URL
          command:
            - sh
            - -ec
            - |
              psql "$DATABASE_URL" -v ON_ERROR_STOP=1 <<'SQL'
              UPDATE games
              SET slug = 'word-hunt',
                  play_url = 'https://word-hunt-arena.win',
                  api_base_url = 'https://word-hunt-arena.win'
              WHERE id = 'a1000000-0000-4000-8000-000000000001';

              UPDATE games
              SET name = 'Rock Paper Scissors Lizard Spock',
                  slug = 'rock-paper-scissors-lizard-spock',
                  description = 'Best-of duel with move cooldown. Single-bucket queue demo.',
                  play_url = 'https://rpsls-duel.win',
                  api_base_url = 'https://rpsls-duel.win',
                  status = 'active',
                  category = 'catalog',
                  manifest_hash = COALESCE(manifest_hash, 'seed-rps-duel-v1'),
                  manifest_synced_at = COALESCE(manifest_synced_at, NOW()),
                  game_version = COALESCE(game_version, '1.0.0')
              WHERE id = 'a1000000-0000-4000-8000-000000000002';

              INSERT INTO game_modes (id, game_id, mode_key, display_name, min_players, max_players, seat_template, status)
              VALUES (
                  'a2000000-0000-4000-8000-000000000002',
                  'a1000000-0000-4000-8000-000000000002',
                  'duel',
                  '1v1 Duel',
                  2,
                  2,
                  '{"count": 2}'::jsonb,
                  'active'
              )
              ON CONFLICT (game_id, mode_key) DO UPDATE SET
                  display_name = EXCLUDED.display_name,
                  min_players = EXCLUDED.min_players,
                  max_players = EXCLUDED.max_players,
                  seat_template = EXCLUDED.seat_template,
                  status = 'active',
                  updated_at = NOW();

              INSERT INTO game_mode_seats (mode_id, seat_key, sort_order)
              VALUES
                  ('a2000000-0000-4000-8000-000000000002', '1', 0),
                  ('a2000000-0000-4000-8000-000000000002', '2', 1)
              ON CONFLICT (mode_id, seat_key) DO NOTHING;

              INSERT INTO mode_queues (id, mode_id, name, players_to_start, status, is_default)
              VALUES (
                  'a3000000-0000-4000-8000-000000000002',
                  'a2000000-0000-4000-8000-000000000002',
                  'Default',
                  2,
                  'active',
                  true
              )
              ON CONFLICT (mode_id, name) DO UPDATE SET
                  players_to_start = EXCLUDED.players_to_start,
                  status = 'active',
                  is_default = true,
                  updated_at = NOW();
              SQL
