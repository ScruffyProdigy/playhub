# Copy to lobby-smtp.yaml (gitignored). Or: kubectl create secret generic lobby-smtp -n joinquest --from-literal=SMTP_PASSWORD='re_...'
apiVersion: v1
kind: Secret
metadata:
  name: lobby-smtp
  namespace: joinquest
type: Opaque
stringData:
  SMTP_PASSWORD: REPLACE_WITH_RESEND_API_KEY
