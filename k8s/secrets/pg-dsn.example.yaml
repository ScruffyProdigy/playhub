# Host: use pg (same namespace) or pg.<namespace>.svc.cluster.local — never an ingress/LB IP.
apiVersion: v1
kind: Secret
metadata:
  name: pg-dsn
  namespace: playhub
type: Opaque
stringData:
  DATABASE_URL: postgres://app:app-pass@pg:5432/playhub?sslmode=disable
