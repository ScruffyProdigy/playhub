# create-joinquest

Wrapper for `npm create joinquest` — runs the same installer as `npx joinquest install`.

```bash
npm create joinquest@latest -- --cursor
JOINQUEST_API_KEY=lq_dev_... npm create joinquest@latest -- --copilot
```
