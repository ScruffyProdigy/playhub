type User {
  id: ID!
  email: String
  displayName: String
  avatarUrl: String
  avatarKey: String
  avatarSource: AvatarSource
  createdAt: Time!
  isAdmin: Boolean!
}

# Public player profile for game servers (no email).
type PublicPlayer {
  id: ID!
  displayName: String
  avatarUrl: String
  avatarSource: AvatarSource
}

enum AvatarSource {
  STARTER
  SPIRIT_ANIMAL
  NONE
}
